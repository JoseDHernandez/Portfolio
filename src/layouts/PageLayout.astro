---
interface Props {
  title: string;
  description?: string;
}
const { title, description } = Astro.props;
import Layout from "./Layout.astro";
---

<Layout title={"JoséDHernández | " + title} description={description}>
  <div class="container"><slot /></div>
</Layout>
